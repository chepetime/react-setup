import React, { Component } from 'react';
import { connect } from 'react-redux';

import './swipper-phones.scss';

function mapStateToProps(state) {
 return {

 };
}

class SwipperPhones extends Component {
 render() {
  return (
   <div></div>
  );
 }
}

export default connect(
 mapStateToProps,
)(SwipperPhones);